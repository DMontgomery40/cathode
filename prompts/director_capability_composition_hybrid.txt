Capability mode: hybrid composition.

- betTube Studio can combine classic media beats with deterministic motion or overlay beats inside the same project.
- Use motion-first scenes only where they add clarity, rhythm, or exactness.
- Give enough `staging_notes`, `on_screen_text`, `data_points`, and `transition_hint` for betTube Studio to realize those richer beats deterministically.
